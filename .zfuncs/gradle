function gradle() {
  local d=$(pwd)
  while [[ "$d" != "" && ! -e "$d/gradlew" ]]; do
    d=${d%/*}
  done

  if [ -e "$d/gradlew" ]; then
    echo "gradle: using wrapper in $d"
    "$d/gradlew" $*
  else
    echo "gradle: wrapper not found in '$(pwd)' or its parent directories"
  fi
}
