function sql()
{
    local db=Measurements

    if [[ $1 = -d ]]
    then
        db=${2}
        echo ${db} > ~/.sql-db
        shift
        shift
    else
        [[ -f ~/.sql-db ]] && db=$( < ~/.sql-db )
    fi

    if [[ $# = 0 ]]
    then
        echo ${db}
        return
    fi

    local COMMAND
    case ${1} in
        sampleattribute)
            shift
            COMMAND="select SA.UID,A.Name,Number,Date,Substring(Text,1,50) as Text from translational_sampleattribute SA, Attribute A where SA.AttributeUID = A.UID  ${*}"
            ;;
        studyattribute)
            shift
            COMMAND="select SA.UID,A.Name,Number,Date,Substring(Text,1,50) as Text from translational_studyattribute SA, Attribute A where SA.AttributeUID = A.UID  ${*}"
            ;;
        visitattribute)
            shift
            COMMAND="select SA.UID,A.Name,Number,Date,Substring(Text,1,50) as Text from translational_visitattribute SA, Attribute A where SA.AttributeUID = A.UID  ${*}"
            ;;
        cohortattribute)
            shift
            COMMAND="select SA.UID,A.Name,Number,Date,Substring(Text,1,50) as Text from translational_cohortattribute SA, Attribute A where SA.AttributeUID = A.UID  ${*}"
            ;;
        subjectattribute)
            shift
            COMMAND="select SA.UID,A.Name,Number,Date,Substring(Text,1,50) as Text from translational_studyattribute SA, Attribute A where SA.AttributeUID = A.UID  ${*}"
            ;;
        study)
            shift
            COMMAND='select * from translational_study '"${*}"
            ;;
        cohort)
            shift
            COMMAND='select * from translational_cohort '"${*}"
            ;;
        subject)
            shift
            COMMAND='select * from translational_subject '"${*}"
            ;;
        visit)
            shift
            COMMAND='select * from translational_visit '"${*}"
            ;;

        sample)
            shift
            COMMAND='select * from translational_sample '"${*}"
            ;;
        row)
            shift
            COMMAND='select distinct MV.UID,A.Name,Number,Date,Text from  measurementvalue MV, measurementcolumn MC, Attribute A where A.UID=MC.attributeuid and MV.measurementColumnUID=A.UID order by MV.UID asc '"${*}"
            ;;
        *)
            COMMAND=${*}
            ;;
    esac
    echo mysql -e "${COMMAND}"  ${db}
    mysql -e "${COMMAND}"  ${db}
}

